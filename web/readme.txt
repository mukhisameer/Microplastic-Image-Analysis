Instructions to host "app.py" as a web page using Flask:

1) Create virtual environment i.e "python -m venv env" and activate the environment
2) Install requirements listed in the requirements.txt file using "pip install -r requirements.txt" command
2) On the terminal go to the "web" directory and run "python -m flask run" 